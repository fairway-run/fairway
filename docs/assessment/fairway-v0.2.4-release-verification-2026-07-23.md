# Fairway v0.2.4 release verification

Date: 2026-07-23

Task: `FW-383`

Result: pass

## Release Identity

- Version and immutable tag: `v0.2.4`
- Source SHA: `5c53402a230a93e0e4bac9255e554956f6b2885a`
- Previous release and rollback reference: `v0.1.13`
- Release workflow: `30033162606`
- CI workflow: `30032959855`
- Docs Portal workflow: `30032961321`
- Public release:
  [Fairway v0.2.4](https://github.com/fairway-run/fairway/releases/tag/v0.2.4)
- Homebrew tap commit: `cc0b3499fe72d61f885ee45e205c0597f5ae6bc7`

The tag was created once at the reviewed source SHA. Rejected `v0.2.0` through
`v0.2.3` candidates remain immutable and unpublished.

## Workflow And Assets

The release workflow passed tests, signing, notarization, release-assurance
generation and verification, and draft creation before the separate publish
step. The public release contains:

- four platform archives for Darwin and Linux on amd64 and arm64;
- the archive checksum manifest;
- the signed release-assurance bundle;
- the portable release-assurance checksum.

Public HTTP `200` readback passed for all seven assets. Archive checksums passed.
The Darwin arm64 binary reports `0.2.4`, and strict `codesign` verification
passes. The release-assurance source provenance binds the artifacts to
`v0.2.4`, source SHA `5c53402a230a93e0e4bac9255e554956f6b2885a`,
the tag-triggered release workflow, and policy `sovereign-release-v1`.

## Homebrew

The public cask reports version `0.2.4`.

```text
brew fetch --cask --force fairway-run/tap/fairway
brew install --cask fairway-run/tap/fairway
```

Both commands passed. `/opt/homebrew/bin/fairway version` reports `0.2.4`, and
the installed binary passes strict macOS code-signature verification.

## Fairway Verification

`fairway release verify` passed with:

- public release state;
- exact source SHA and tag;
- CI, docs, signing, and notarization status `pass`;
- all seven public asset checks returning `200`;
- Homebrew version `0.2.4`;
- tap commit and brew fetch status;
- the release-assurance source-provenance evidence.

## Upgrade And Rollback

A disposable project was initialized with the released `v0.1.13` binary and a
todo task. The released `v0.2.4` binary then:

1. validated the existing configuration;
2. classified the old contract as `legacy_unmanaged`;
3. produced a non-mutating adoption plan;
4. applied `--adopt-legacy`;
5. reported managed schema `1`, revision `2026-07-23.1`, and status `current`;
6. preserved the task and returned clean active reconciliation.

Restoring the pre-upgrade database and managed contract, removing the
upgrade-created local contract, and running the released `v0.1.13` binary
returned the project to the same todo task and clean reconciliation state.

## GPUaaS Adoption

GPUaaS adopted the released managed agent contract in commit
`3bd346ffd65f6bc4ed5ece78a79d80ca8d447d6e`. The repository now contains:

- `.fairway/AGENTS.md`, generated by Fairway `0.2.4`, schema `1`, revision
  `2026-07-23.1`;
- a minimal root `AGENTS.md` bootstrap that directs agents to the managed
  contract while retaining GPUaaS-specific engineering rules.

Post-adoption contract status is `current`, configuration validation passes,
knowledge lint reports zero findings, and the adoption boundary was pushed to
GPUaaS `master`.

## External Boundaries

Legal, export-classification, and independent-assessment tasks remain blocked
on qualified external owners. They do not block the general `v0.2.4` release,
and this verification makes no certification, jurisdiction, export, or
independent-assurance claim.

## Verdict

Fairway `v0.2.4` is public, installable, source-bound, signed, notarized,
assurance-packaged, upgrade/rollback rehearsed, and adopted by GPUaaS. The
release is ready for normal use.
